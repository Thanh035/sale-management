export interface RoleDTO {
  id: number;
  groupName: string;
  groupCode: string;
  createdDate: Date;
  createdBy: string;
}

export interface RoleDetailDTO {
  name: string;
  code: string;
  createdDate: Date;
  createdBy: string;
}
