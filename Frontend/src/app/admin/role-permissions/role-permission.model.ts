export interface RoleDTO {
  id: number;
  name: string;
  code: string;
  createdDate: Date;
  createdBy: string;
}

export interface RoleDetailDTO {
  name: string;
  code: string;
  createdDate: Date;
  createdBy: string;
}
